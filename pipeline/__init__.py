"""Grimoire compendium pipeline — desktop-only build tooling.

`python3 -m pipeline all` = fetch → normalize → validate → emit → legal → fixtures.
Never runs on the phone or on Light's build server; its outputs are committed."""
